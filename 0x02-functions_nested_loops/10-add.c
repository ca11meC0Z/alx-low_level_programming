#include "main.h"
/**
 * add - adds two numbers and gives the result
 * @y: is a natural number the user puts in
 * @x: is a natural number the user puts in
 * Return: Result
 */
int add(int x, int y)
{
	int z = (x + y);

	return (z);
	_putchar('\n');
}
