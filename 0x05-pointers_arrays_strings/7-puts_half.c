#include "main.h"
/**
 * puts_half - prints last half of the string
 * @str: string we are working on
 **/
void puts_half(char *str)
{
	int len = 0;
	int m, n;

	while (m = 0; str[m] != '\0'; m++)
	{
		len++
	}
	n = len / 2;
	if len % 2 = 1
		n = (len + 1) / 2;

	for (m = n; str[m] != '\0'; m++)
		_putchar(str[m]);
		_putchar('\n');
}
